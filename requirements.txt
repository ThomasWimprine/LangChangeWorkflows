# LangGraph Learning Journey - Minimal Dependencies
# Install with: pip install -r requirements.txt

# Core Framework
langgraph>=0.2.0           # State machine framework for LLM workflows

# Claude API
anthropic>=0.40.0          # Anthropic Claude API client

# Environment Configuration
python-dotenv>=1.0.0       # Load .env files

# Testing (for later lessons)
pytest>=8.0.0              # Testing framework

# Note: Additional dependencies will be added as we progress through lessons
# - pytest-cov (Lesson 06: Testing and coverage)
# - pyyaml (Lesson 07: Configuration management)
# - jsonschema (Lesson 09: Schema validation)
