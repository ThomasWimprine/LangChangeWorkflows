# LangGraph PRP Workflow Requirements
# Install with: pip install -r requirements.txt

# Core LangGraph and LangChain
langgraph>=0.2.0
langchain-anthropic>=0.2.0
anthropic>=0.40.0

# Environment and Configuration
python-dotenv>=1.0.0
pyyaml>=6.0.0

# Testing Tools (for Gate 2: Coverage)
pytest>=8.0.0
pytest-cov>=5.0.0

# Optional: Enhanced Features
jsonschema>=4.0.0          # For PRP schema validation
black>=24.0.0              # Code formatting
ruff>=0.6.0                # Fast Python linter
